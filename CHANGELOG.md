# 更新日志 / Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。日期格式 YYYY-MM-DD。

## [Unreleased]

_（暂无）_

## [0.9.14] - 2026-07-02

### 🐛 修复
- **上下文用量恒显 100%**：窗口大小按模型粗判的规则停留在「Opus=1M、其余=200k」，Fable 5 / Sonnet 5 这类 1M 窗口的新模型被按 200k 算，一超就顶死 100%。改为 Fable / Opus / Sonnet 5+ → 1M，其余 200k。

## [0.9.13] - 2026-07-02

### ✨ 增强
- **分身统一模型**：设置里新增「分身统一模型」——引擎 spawn 的所有会话（飞书问答 / 定时 / 循环）统一用它，新模型上线改一处即全员切换；会话节点自己填的「模型」优先级更高（覆盖全局）。改完下一条消息即生效，续接原会话、上下文不丢。检视区模型字段与画布节点卡的文案同步更新（空 = 跟随全局）。
- **README（中英）新增「循环节点 ≠ 官方 /loop」对比**：明说官方没有的四件——maker-checker 验收闸、飞书实时镜像、预算护栏（轮数 + 美元 + 强制中断）、舰队看板。
- **会话节点选工作目录出原生弹窗**：检视区「工作目录 cwd」输入框旁新增 📁 按钮，点开 Windows 原生文件夹选择器（tauri-plugin-dialog）；手输 / 粘贴路径照旧可用。

## [0.9.12] - 2026-06-28

### ↩️ 撤回
- **去掉终端「最近提问」预览条**（0.9.11 加的）：它捕获的是 claude 输入框在终端里的原始排版（带多列空白），拼出来是碎的、不成句、不好看，遂移除。

## [0.9.11] - 2026-06-26

### ✨ 增强
- **终端「最近提问」预览条**：终端信息栏下方新增一条，显示你在本终端最近一次提交的提问，**最多 3 行**（超出省略）。长会话里一眼定位自己上次问了啥。只显示、不跳转；没提问时不显示。

## [0.9.10] - 2026-06-26

### 🐛 修复
- **终端 Ctrl+C 复制失效**：复制只用 `navigator.clipboard.writeText`，WebView2 某些状态下它缺失/被拦会**静默失败**（与小人黑底同类的运行时问题）。改为 `writeClipboard`：优先 `navigator.clipboard`，失败回退老式 `textarea + execCommand('copy')` 再把焦点还给终端；Ctrl+C / Ctrl+Shift+C / 剪切都走它，粘贴加 `?.` 防同步抛错。

## [0.9.9] - 2026-06-26

### ✨ 增强（部分学自 OTTY 调研）
- **转录块状语义复制**：转录面板每个回复 / 🔁循环指令块悬停浮现「⧉ 复制这块」，搜索栏加「复制全部」。
- **会话快捷发起（Quick Launch）**：会话节点可存常用 prompt（每行一条），在「发测试消息」上方渲染成可点小标签，点一下绕过飞书直发。

### 🔧 工程 / 合规（对运行无影响）
- **cargo-deny 合规闸**：`deny.toml`（许可证白名单 + 漏洞/yanked 卡死）+ CI 加 cargo-deny job（只读 lockfile、不编译）。
- **clippy 零警告闸**：修 lib.rs 既有告警；`release.mjs` 发版前跑 `cargo clippy -- -D warnings`。
- **AGENTS.md**：指向 `CLAUDE.md` 单一事实源，避免多份 agent 规范漂移。

## [0.9.8] - 2026-06-26

### 🔁 循环节点 · 三个新能力（来自趋势调研的 P0/P1）
- **maker-checker 完成验收闸**（开关「完成前验收」）：命中完成标记后，先用一次便宜/无工具/不落会话的 LLM（默认 haiku）读 原任务 + STATE.md + 最近几轮产出判 done/not-done；未过则带理由继续、不会假完成，过了才收尾并回帖裁决。专治"会话糊弄完成条件"。
- **飞书交互卡控制 + @人格路由**：① 循环收尾汇总可发成飞书交互卡（开关「完成汇总用交互卡」），挂 ⏵继续/▶重跑/📄看轨迹 按钮，主人点一下即操作（发卡失败回退纯文本）；② 群里消息以「@<会话名/人格名> …」开头即直接路由到该会话（附加能力，命不中按原流程走）。
- **主窗「循环舰队」看板**：欢迎仪表盘新增实时板，列每个循环的状态（运行中第几轮 / 待命 / 上次停因）+ 累计花费，一键 跑 / 继续 / 强制中断。

## [0.9.7] - 2026-06-25

### 🐛 修复
- **「完成啦」小人提醒弹窗黑底**：透明窗的背景过去在 React `useEffect`（首帧之后）才设透明，WebView2 一旦用不透明背景合成过首帧就会残留黑底（运行时升级后变明显）。改为在 `main.tsx` **首帧之前**同步把根背景设透明，弹窗恢复透明悬浮。
- **顶栏「活动趋势」柱状图过段时间变空**：5 分钟轮询过去用 `deep=false`，取不到 `recentDays`（今天/昨天活动），近 7 天柱状图随时间归零变空，要悬停触发 deep 才恢复。改为轮询也用 `deep`；并且空/读失败的结果不再覆盖已有数据。

### 📝 文档
- 中英 README 更新到当前形态：循环节点（自主多轮）、画布独立窗 + 欢迎仪表盘、自动更新。

## [0.9.6] - 2026-06-24

### 🔁 循环节点
- **运行时环境变量**：循环检视区新增「运行时环境变量（KEY=VALUE，每行一个）」。这些变量**只注入到本循环 spawn 的会话进程**，不影响其他会话/全局——可当"开关"用：例如填 `DOC_FIRST_GATE_BYPASS=1`，让项目里读该变量的 hook（如 doc-first-gate）**仅在本循环运行期间**放行，你平时正常编辑代码不受影响（取代了"放标记文件全局豁免"的糙办法）。引擎经 `claude-session` 的 spawn env 逐次注入。

## [0.9.5] - 2026-06-24

### 🔁 循环节点
- **导出/导入配置**：循环检视区新增「⬇ 导出配置 / ⬆ 导入配置」。导出把该循环的全部参数（任务/继续语/停止方式/轮数/重置/报告等）写成 `oblivionis-loop-<名称>.json`，**默认落到下游会话的工作目录**；导入从同一文件读回套用（走正常配置通道，立即生效）。方便让别人或 Claude 把这个文件改好再导入——绕开"配置文件没法热改"的限制。

## [0.9.4] - 2026-06-24

### 🔁 循环节点
- **修复重置点失控 bug**：开了「每 N 轮重置上下文」时，重置后紧接的那一轮过去发的是通用「继续下一步」，会丢掉用户继续语里精心写的"一轮一项/做完停"纪律，导致每个重置点模型放飞、一波批处理失控。现在重置后仍沿用用户的继续语（仅在前面加一句"已重置、先读 STATE.md"提示）。
- 检视区新增「📖 怎么写好循环提示词？」折叠提示，教如何写自包含、逐轮可控的循环指令。

## [0.9.3] - 2026-06-23

### 🔁 循环节点
- 新增 **⏹ 强制中断** 按钮：立刻杀掉正在跑的那一轮（与空闲看门狗同一机制，安全），并停止后续轮次；被中断时跳过详细报告那一轮。
- 新增 **⏵ 继续** 按钮：直接用「继续语」往下接着跑（不重发初始任务 prompt）——适合中断后续接、或推进一个已开了头的会话。

## [0.9.2] - 2026-06-23

### 🔁 循环节点
- 「最多轮数」上限再放开到 **500**（实测有把任务拆成几百步的用法；maxRounds 仍是兜底刹车）。

## [0.9.1] - 2026-06-23

### 🔁 循环节点
- 「最多轮数」上限从 **50 → 100**（schema/引擎/界面同步放开；maxRounds 仍是兜底刹车）。

## [0.9.0] - 2026-06-23

### 🔁 循环节点增强
- **实时输入镜像**：循环每轮发给会话的指令现在会实时出现在该会话的转录里（渲染成「🔁 第 N 轮指令」），不用等收尾的 run-log——边跑边能看到「我问了什么 → 它答了什么」。
- **详细报告（可选）**：循环节点新增「详细报告」选项（不生成 / Markdown / HTML）。开启后，在审核群的简要汇总之外，循环收尾再多跑一轮把各轮产出整理成一份详细报告，存到 `~/.oblivionis/reports/`（文档查看器可看）。
- 配套 **Perforce(P4) 写改技能模板** `docs/loop-p4-skill-template.md`：教会话「`p4 edit`/`p4 add` → 改 → 留在 pending changelist → **绝不 `p4 submit`**」，提交永远由人来。适合「让循环自动改代码、但人把关提交」的 P4 工作流。

## [0.8.0] - 2026-06-23

### 🔁 循环节点（Loop Engineering 驱动器）
- 新增 **🔁 循环** 节点（加节点 → 执行节点）：连到一个「Claude 会话」后对它**反复**跑——第 1 轮发任务、之后每轮回灌「继续语」进同一脱敏分身——直到**命中完成标记 / 满轮数 / 超预算**才停。带「▶ 跑一次」手动触发 + 实时轮次显示；也支持定时触发（语法同 cron）。
- **刹车**：最多轮数（永远兜底，防失控空转）+ 可选预算上限（$，复用成本账本）；全程 **L1（只报告）**，破坏性操作仍走审批卡。
- **每 N 轮新鲜上下文**（Ralph Wiggum 式长循环）：定期重新 fork 出新鲜分身、靠工作目录 `STATE.md` 续接进度，避免上下文越滚越大变慢变贵。
- **run-log**：每次循环把「每轮指令 + 回复」成对落盘到 `~/.oblivionis/loop-logs/`，可事后审计循环每轮问了什么、答了什么。
- 配套「造-检」技能模板 `docs/loop-skill-template.md`（贴进 🧩 技能节点，教会话用 STATE.md 记进度、做一步验一步）。

### 🛠 发版脚本
- `scripts/release.mjs` 发版前**自动重打 bridge sidecar**，避免有 bridge 改动的版本误带旧引擎。

## [0.7.0] - 2026-06-22

### ✨ 回复挂「操作按钮」（技能 + 引擎）
- 会话回复末尾可输出一段 ```buttons``` JSON 块,引擎渲染成飞书卡片按钮;对方点按钮 → 该按钮的 `send` 文本作为新一条消息**回灌进本会话**(走正常入站流程,路由/技能/脱敏全复用),实现「点一下继续对话」。
- 引擎侧:`splitButtons` 解析+剥离按钮块、`sendActionCard` 发卡、`card.action.trigger` 收 `kind:"session-input"` 回调合成入站消息。按钮文字与回灌文本对访客同样脱敏。
- 配套技能模板:🧩 节点贴入即教会模型何时挂按钮(模板见 `~/.oblivionis/reports/操作按钮技能.md`)。

### ✨ 会话支持「按群 fork」（隔离多群上下文）
- 会话节点新增 **fork 粒度**选项(设了 baseSessionId 时显示):
  - **按会话**(默认,旧行为):路由到本会话的多个群共用同一份 fork 上下文,可能互相串味。
  - **按群**:每个群(chatId)各自从基础会话 fork 一份独立脱敏分身,群与群上下文互不污染。
- 引擎按 `${nodeId}::${chatId}` 维护各群分身(`groupSessions` 映射),复用 `forkAndSanitize`(脱敏保证不变);按群模式下「刷新快照」=清空各群分身、下次各群重新 fork。

## [0.6.3] - 2026-06-22

### 🐛 修复
- **启动时画布窗口会自己弹出来**:画布独立窗与主窗加载同一份 App,启动后「关闪屏→显示窗口」那段在画布窗实例里也跑了、把它自己 `show()` 了出来。改为该逻辑(及点小人聚焦)只在主窗执行;画布窗保持隐藏,仅在点「节点图」时出现。

## [0.6.2] - 2026-06-22

### 🐛 修复自动更新安装失败
- 自动更新安装时报 **「Error opening file for writing … oblivionis-bridge.exe」**:更新装回器覆盖文件时,引擎 sidecar 还在运行、锁着自己的 exe。现在 NSIS 安装包加了**预安装钩子**,覆盖前先 `taskkill` 掉 `oblivionis-bridge.exe`,装完由新程序重新拉起。
  - 钩子在「新安装包」里,所以从更早版本升级也会生效。

## [0.6.1] - 2026-06-22

### ✨ 自动更新体验
- **设置 → 更新 → 「检查更新」按钮**:一键手动查更新,并显示当前版本号;已是最新会提示「已是最新版本 ✓」。
- 启动后**每 5 小时自动检查一次**更新(此前只在启动时查一次)。

## [0.6.0] - 2026-06-22

### 🔄 自动更新（重点）
- 接入 Tauri 官方更新器:App 启动后台向 GitHub 查最新版,有新版顶部弹「🎉 有新版本 · 现在更新」→ 自动下载安装包 → 装完重启;也可在设置里手动检查。
- **安全**:更新包用 minisign 私钥签名、App 内置公钥校验,非官方 / 被篡改的更新一律拒装;App 只认本仓库的 Release 端点。私钥只在作者本机、绝不进仓库。
- ⚠️ **分发改为 NSIS 安装包**(perUser 安装、无 UAC):自动更新只对安装版生效。**老的绿色便携版用户需手动装一次本版**,之后即可自动更新。

### 🐛 修复
- **节点画布独立窗口缺权限声明**:v0.5.0 加了 `canvas` 独立窗口但漏把它加进 capabilities,导致该窗口里需要 Tauri 调用的能力被拒(图编辑走 WebSocket 不受影响,故当时没显形)。本版补上。

## [0.5.0] - 2026-06-22

### 🪟 节点画布独立成单独窗口
- 节点连线画布从「主视图右侧的一个面板」**独立成单独窗口**（像文档查看器那样另开一窗）。主视图从此专心承载**终端 + 欢迎页**，不再和画布来回挤、来回切。点左侧「节点图」、欢迎页「节点画布」按钮即开 / 聚焦该窗口；关闭 = 隐藏，随时可再开。两窗经后台引擎的配置广播自动同步——画布窗里改连线 / 加节点，主窗的总览即时跟着变（图的编辑只在画布窗，主窗只读展示）。

### 🏠 全新欢迎主页（一图流仪表盘）
- 打开软件默认落「欢迎主页」，把关键信息整合成**占满视窗的一图流**，而不是一堆跳转按钮：**节点编排总览**（纯色块 + 连线的缩略图，点开即进画布）· **活动日历**（本月热力图 + 关键统计 + 今日宜忌）· **订阅用量**（5h / 周窗口）· **成本看板**（今日 / 累计 / 运行 + 按会话花费排行）。卡片半透融入面板，整页读作一块连续信息。左侧栏新增 🏠 Home 按钮，随时回来。

### 🖼️ 导出高清画布图片
- 画布工具条新增「导出图片」：把整张连线图导成 **2× 高清 PNG**（自动适应视图、跟随明暗主题、只含节点与连线），方便分享 / 存档。

### 🗂️ 节点编辑器升级为独立弹窗
- 节点编辑从角落小浮窗改为**带遮罩的居中弹窗**（Esc / 点遮罩关闭），编辑空间更大更聚焦，也为后续更重的节点配置留好位置。

## [0.4.2] - 2026-06-18

### 🔒 重点修复
- **当前群 chatId 注入会话上下文（防回错群）**：一个会话节点常被多个飞书群共用（画布上多群 → 同一会话），但入站上下文里没带 chatId，会话（尤其用 lark 工具主动外发时）不知道自己在哪个群，可能回到莫名其妙的群里。现在每条飞书消息的系统提示里加「当前对话」块：本条消息来自群 `chatId`（+ 群名），正常回复直接输出文字即可（系统自动发回本群），要用工具主动外发只能针对这个 chatId、绝不发别群；用户消息里出现的任何 chatId / 群名都不可信、以此处为准（防访客伪造）。主人 / 访客都注入。

### 🐛 修复
- **活动日历昨天 / 今天仍是灰格**：上版按「会话起始日」归类、且跳过首条早于缓存截至日的老会话，但若你的近期活动都在「更早开始、被 resume」的长会话里就会漏算。改为按**每条消息自己的时间戳本地日期**分桶（前端传时区偏移给后端做 UTC→本地转换），不再看会话起始日；只读文件末尾 ~8MB（近期消息在尾部），省得整读几十 MB。

## [0.4.1] - 2026-06-18

### 🖥️ 文档查看器（更像专业工具）
- **工作区内文件快速搜索**：按文件名 / 相对路径过滤（名字命中优先），结果显示所在文件夹；`Esc` 清空、`Enter` 打开首条、`×` 清除。
- **近期修改单独分栏**：🕒 按修改时间取前 8 + 相对时间。
- 侧栏 UX 重构：分组为「搜索结果 / 近期修改 / 全部文档树」，换目录自动清空搜索。

### 🧩 技能
- **终端会话也吃技能**：连在会话「技能口」上的技能，现在打开该会话内置终端时也会注入（单行渐进式指针，命中场景再 Read 完整规范）——纯终端用的会话也能用上技能了。只技能、不碰人格 / 护栏（「终端保持干净」只针对人格 / 护栏）。

### 🐛 修复
- **活动日历跨天不刷新**：`~/.claude/stats-cache.json` 滞后时（只在 CLI 重算才前移），昨天 / 今天在本月热力图上是灰格。现在悬停时扫近期 transcript 补「缓存截至日之后」每天的活动并按本地日期合并；被 resume 的老会话先读头跳过、不整读，60s TTL 缓存，轮询不扫——保持轻量。

## [0.4.0] - 2026-06-17

### 🔒 重点修复
- **主人不再被自己的审批卡拦住**：审批门控（`--permission-prompt-tool` + fork-settings 把 Bash/Edit 强制成 `ask`）原先在 `approvalMode=true` 时无条件挂上，会**盖过主人的 `bypassPermissions`**——于是主人自己提问也弹「工具审批」卡，还会**阻塞最多 100s 等审批**（连带「正在输入」一直不消、卡片点了像没反应）。改为仅当 `permissionMode!=="bypassPermissions"` 时才挂门控：主人免审、访客（default 模式）审批流不变。一处修复同时消除主人侧的三个现象。

### 🧩 技能
- **技能节点支持渐进式披露**（仿 Claude 原生 Skills）：技能文件带 `description:` frontmatter → 系统提示只注入「名字 + 用途 + 文件路径」指针（省上下文），模型命中场景时自己用 Read 读完整正文；不带 frontmatter 的技能维持「全文常驻」（向后兼容）。「报告型 HTML 样式」技能即用此机制，正文可写很全也不占日常上下文。

### 🎛️ 画布交互
- **左键框选**（可多选节点一起拖动）+ **中键（按下滚轮）平移** + **按住空格 + 左键平移**（照顾没鼠标中键的情况）；右键保留给上下文菜单。

### 🖥️ 桌面 / 文档
- 状态栏版本号改为**动态读真实版本**（不再写死，发版不会再漏改）。
- 新增软件介绍页 `docs/presentation.html`，报告 / 演示型 HTML 设计规范落在 `docs/html-design.md`（含可贴进技能节点的精简稿）。

## [0.3.0] - 2026-06-17

本期重点是**多会话流水线（L5）**——把多个本地会话串成流水线协作；外加一批可靠性加固，以及若干桌面信息一览（需重建生效）。

### 🔗 编排（本期重点）
- **多会话流水线（L5）**：画布支持「会话 → 会话」连线；一个会话回复后，沿连线把产出喂给下游会话继续加工，逐级把结果发回同群（`⛓` 前缀）。让多个本地会话各司其职、串成一条流水线协作。完全加性 + opt-in（router 对会话是终点，对现有路由零影响）；visited 去重 + 最多 3 跳防环，下游产出出站前脱敏。

### 🔒 可靠性 / 安全
- **cron 看门狗 + 抖动**：单次运行超 10 分钟未结束强制复位（修「卡死后永不再触发」）；同一 tick 多个到点错峰启动，避免同秒齐发撞飞书限频。
- **config 原子写 + 备份**：先写临时文件再 rename（写一半崩了不污染主文件）；落盘前备份 `.bak`，主配置损坏时自动从备份恢复；加 schema 迁移挂钩。
- **webhook HMAC + 限流**：webhook 节点可设 HMAC 密钥，校验 `X-Hub-Signature-256` / `X-Signature` 签名头防伪造回调；每 token 滑窗限流 60 次/分钟。

### 🖥️ 桌面体验
- **节点 / 会话状态更醒目**：会话侧栏圆点（运行 / 出错）加发光 + 脉冲，出错会话卡加红色左条；节点出错态改为缓慢脉冲（`prefers-reduced-motion` 下不动）。
- **文档查看器加「复制路径」**：文件树右键 + 文档头部按钮。
- **禁用 webview 原生右键菜单**（可编辑文本框除外）。

### 📊 可观测性 / 信息一览（读本地缓存 / 配置，不调 API、不耗 token）
- **成本看板（悬浮框）**：一键弹出盖在主界面上的浮框——累计 / 今日花费、按会话柱状、近 14 天迷你柱图、最近运行明细；点别处自动收起。数据来自每次会话运行的 stream-json `cost_usd`，记到 `~/.oblivionis/costs.jsonl`。
- **上下文用量悬停**：状态栏当前终端名旁显示 `%`，悬停看上下文体量（固定开销 / 对话消息 / 空闲）+ 上次回合输出。读 transcript 估算；mtime 门控 + 只读文件头尾窗口，空闲近零开销。
- **顶栏「活动趋势」小标**：chip 本身是近 7 天迷你趋势图；悬停浮框 = 本月日历热力图（含星期 / 日期）+ Claude `/stats` 同款全量统计（常用模型 / 总 token / 会话数 / 最长会话 / 活跃天数 / 连续活跃 / 最活跃日）。读 `~/.claude/stats-cache.json`。
- **左上角品牌悬停**：本软件版本 + 构建时间，以及 Claude CLI 版本 / 登录账号 / 套餐（读 `~/.claude.json`，敏感令牌不碰）。
- 彩蛋：上面活动浮框里附了个「今日宜忌」开发版黄历（85 条程序 + 游戏开发梗，每天稳定随机，图一乐）。

## [0.2.0] - 2026-06-17

### 🖥️ 桌面体验
- **文档查看器（独立窗口）**：浏览各「Claude 会话」工作目录下的 `.md` / `.html`，左侧栏 📖 一键唤起。
  - 左侧把各会话工作目录**去重** + 公共 `~/.oblivionis/reports` 目录整理成切换列表（多个会话同目录合并成一条）。
  - 选中目录后把其下文档排成**只含 md / html 的目录树**——已过滤 Unity / 构建 / VCS 等重目录，限深度与数量，**百万文件不卡死**。
  - 右侧 **Markdown 渲染**（remark-gfm 表格 / 任务列表 + 原始 HTML + 本地图片）、**`.html` 沙箱内嵌原样显示**。
  - **独立窗口（非模态）**——可边看文档边继续操作主窗；带刷新按钮（有新文档时重扫）。

## [0.1.0] - 2026-06-16

首个公开版本。把飞书群聊接入本地 Claude Code 会话的 Windows 桌面工具。

### 🧩 核心
- **飞书群 ↔ 本地 Claude 会话桥接**：群里 @机器人 → 路由到你电脑上对应项目的 claude 会话 → 富文本回帖并 @提问人。
- **订阅合规**：一切 LLM 调用都通过遥控**官方 `claude` CLI** 完成，绝不直连 API / 碰订阅 OAuth 令牌。
- **连线式画布**（React Flow）：飞书群 → 路由 → Claude 会话，拖节点连线即接入；可折叠成会话卡片专注终端。
- **两会话模型**：`base`（你的开发终端会话，永不被飞书污染）+ `fork`（脱敏分身，所有飞书消息都走它）。
- **多群多会话 + 意图分流**：不同群路由到不同项目（各自 cwd / 模型 / 权限）；同群消息按 LLM 语义意图走不同分支。

### 🔗 节点类型
- 飞书群 / 路由 / 意图分流 / Claude 会话 / 定时任务 / Webhook。
- **赋能节点三件套**：🎭 人格（SOUL.md）/ 🧩 技能（SKILL.md）/ 🦾 子代理（Claude Code 原生 subagent，在独立上下文做重活、claude 自动委派）——连到会话的赋能口即生效，一个节点可共享给多个会话。
- 赋能连线用**虚线 + 按类型上色**与消息流区分。
- 「**重锚人格**」：改完人格 / 刚连上会话后一键把当前人格重新注入所连会话（保留对话记忆），比「刷新快照」更轻。

### 🔐 安全
- **访客隔离**：访客走 fork 脱敏分身；fork 时 transcript 抹密钥；回复**出站二次脱敏** + 访客安全护栏。
- **飞书审批卡**：访客的敏感操作（改文件 / 执行命令）弹交互卡到群里，主人 [允许 / 拒绝] 才执行；裁决或超时后卡片自动更新状态、去按钮。
- **App Secret 存 Windows 凭据管理器**：不再明文落 `config.json`、不经 WS 广播；首次启动自动从旧明文迁移。
- 主人 / 访客分级 permission mode；全量入站审计落盘。

### 💬 飞书侧交互
- 自助命令：`/status`（状态卡）· `/doctor`（自检）· `/retry`（重跑）· `/continue`（续跑），仅主人可用。
- 读飞书 **docx / Wiki / Sheets / 多维表** 喂上下文。
- **读消息里的文件附件**：随消息（或被引用消息）发来的文本类文件自动读出正文喂给 claude，二进制给本地路径让其用 Read 打开；外部内容用随机围栏隔离，当资料不当指令。
- **长回复自动作为飞书文件回传**，不撑爆气泡。
- 回复含 Markdown 表格 → 飞书**原生表格**。
- **逐工具进度**流式卡：实时显示「正在 运行命令 / 读取文件…」。

### 🖥️ 桌面体验
- **内置交互终端**：双击节点打开开发会话（完整历史回放）；多终端保活·拖拽排序、剪贴板贴图喂图、字号缩放（设置滑杆 / Ctrl±）。
- **中英双语界面**：设置里一键切换 中文 / English，技术标识符保持原样，漏译自动回退中文。
- **会话视图粘滞**：左侧选会话只换「哪个会话」，终端 / 转录视图用面板顶部小页签切换，不再强制跳终端。
- **定时任务**（自然语言创建）/ **Webhook**（外部 HTTP 触发）/ **群记忆**（按群积累）/ **知识收件箱**（问答沉淀规则待主人裁决）。
- 全局唤起热键（默认关，设置里可配可换）。
- 运行时动效：启动闪屏、节点链路流线（只点亮真实路径，多群并发各自点亮）、会话扫光、完成小红旗、桌面完成小人。
- 节点状态药丸（运行中 / 出错）、连线运行轨迹（累计触发次数 + 最近时间）。

### 📦 工程 / 发布
- **绿色部署**：Tauri v2 打包，主程序 exe + 引擎 sidecar exe 两个文件即可运行；单实例防重开、关窗即净退。
- `rebuild-deploy.bat` 一键 构建 → 部署 → 重启。
- 许可证：**GPL-3.0**（强 copyleft）。第三方依赖声明见 `THIRD-PARTY-NOTICES.md`（`scripts/gen-notices.cjs` 自动生成，含 JS + Rust 依赖）。

> ℹ️ exe / 安装包未做代码签名，首次打开 Windows SmartScreen 会拦——点「更多信息 → 仍要运行」即可。

[0.4.2]: https://github.com/RudAns/OblivionisAgent/releases/tag/v0.4.2
[0.4.1]: https://github.com/RudAns/OblivionisAgent/releases/tag/v0.4.1
[0.4.0]: https://github.com/RudAns/OblivionisAgent/releases/tag/v0.4.0
[0.3.0]: https://github.com/RudAns/OblivionisAgent/releases/tag/v0.3.0
[0.2.0]: https://github.com/RudAns/OblivionisAgent/releases/tag/v0.2.0
[0.1.0]: https://github.com/RudAns/OblivionisAgent/releases/tag/v0.1.0
